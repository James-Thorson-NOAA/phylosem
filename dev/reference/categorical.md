# Family for categorical data

Allows using `family = categorical()` to specify factor-valued traits

## Usage

``` r
categorical(group)
```

## Arguments

- group:

  string indicating which trait a given category belongs to
